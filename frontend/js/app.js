class LearningPlatformApp{constructor(){this.activeTab='dashboard'}init(){document.querySelectorAll('[data-tab]').forEach(b=>b.addEventListener('click',()=>this.switchTab(b.dataset.tab)));this.dashboard=new DashboardComponent(apiClient,t=>this.switchTab(t));this.gap=new CompetencyGapComponent(apiClient,id=>{document.getElementById('training-employee-id').value=id;this.switchTab('training');this.training.loadRecommendations(id)});this.training=new CourseRecommendationComponent(apiClient);this.quiz=new QuizGeneratorComponent(apiClient);this.dashboard.load()}switchTab(tab){['dashboard','gap-analysis','training','quiz'].forEach(x=>{document.getElementById(`${x}-pane`)?.classList.toggle('active',x===tab)});document.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));this.activeTab=tab;window.scrollTo({top:0,behavior:'smooth'})}}document.addEventListener('DOMContentLoaded',()=>{window.app=new LearningPlatformApp;window.app.init()});
