class CourseRecommendationComponent{constructor(apiClient){this.apiClient=apiClient;this.form=document.getElementById('training-recommend-form');this.input=document.getElementById('training-employee-id');this.out=document.getElementById('training-results-container');this.form?.addEventListener('submit',e=>{e.preventDefault();this.loadRecommendations(this.input.value)});document.getElementById('training-quick-select')?.addEventListener('click',e=>{const b=e.target.closest('[data-id]');if(b){this.input.value=b.dataset.id;this.loadRecommendations(b.dataset.id)}})}async loadRecommendations(id){if(!id)return;try{const d=await this.apiClient.getRecommendedCourses(id);this.out.hidden=false;this.out.innerHTML=`<div class="gov-card"><h3>Learning path for ${this.#esc(d.employeeName)}</h3><p>${d.missingSkillsCount} competency gaps</p><div class="courses-grid">${(d.recommendedCourses||[]).map(c=>`<article class="course-card"><div class="course-ribbon"></div><div class="course-body"><div class="course-provider">${this.#esc(c.provider)}</div><h4 class="course-title">${this.#esc(c.title)}</h4><div class="course-badges"><span class="course-badge">🎯 ${this.#esc(c.skillTargeted)}</span><span class="course-badge">⏱ ${this.#esc(c.estimatedDuration)}</span></div></div><div class="course-footer"><a href="${this.#safe(c.url)}" target="_blank" rel="noopener" class="btn btn-primary btn-sm">Open course ↗</a></div></article>`).join('')}</div></div>`;window.lucide?.createIcons?.()}catch(e){this.out.hidden=false;this.out.innerHTML=`<div class="gov-card empty-state">${this.#esc(e.message)}</div>`}}#safe(u){return /^https?:\/\//i.test(String(u||''))?String(u).replace(/"/g,'&quot;'):'#'}#esc(v){return String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')}}
