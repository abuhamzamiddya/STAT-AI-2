class DashboardComponent {constructor(apiClient,onNavigate){this.apiClient=apiClient;this.onNavigate=onNavigate;this.container=document.getElementById('dashboard-pane')}async load(){try{this.#render(await this.apiClient.getDashboardOverview())}catch(e){this.container.innerHTML=`<div class="gov-card dashboard-error">Dashboard unavailable: ${this.#esc(e.message)}</div>`}}#render(d){const rows=(d.employeesNeedingAttention||[]).map(e=>`<tr><td><strong>${this.#esc(e.employeeName)}</strong><span>${this.#esc(e.designation)}</span></td><td>${e.competencyScore}%</td><td>${e.missingSkills}</td><td><span class="priority-badge ${String(e.priority).toLowerCase()}">${this.#esc(e.priority)}</span></td><td><button class="table-action" data-employee="${e.employeeId}">Analyse →</button></td></tr>`).join('');const gaps=(d.topSkillGaps||[]).map(g=>`<div class="gap-insight"><div><strong>${this.#esc(g.skill)}</strong><span>${g.affectedEmployees} employees affected</span></div></div>`).join('');this.container.innerHTML=`<div class="dashboard-heading"><div><p class="eyebrow">Workspace overview</p><h2>Learning intelligence, at a glance.</h2><p>Use competency data to focus learning where it matters most.</p></div><button class="btn btn-outline btn-sm" id="dashboard-refresh">Refresh data</button></div><div class="metric-grid"><article class="metric-card"><div><span>Total employees</span><strong>${d.totalEmployees??0}</strong></div></article><article class="metric-card"><div><span>Learning materials</span><strong>${d.activeLearningPrograms??0}</strong></div></article><article class="metric-card"><div><span>Average readiness</span><strong>${d.averageCompetencyScore??0}%</strong></div></article><article class="metric-card"><div><span>Critical attention</span><strong>${d.criticalSkillGaps??0}</strong></div></article></div><div class="dashboard-grid"><section class="gov-card"><h3 class="gov-card-title">Employees needing attention</h3><div class="responsive-table"><table><thead><tr><th>Employee</th><th>Readiness</th><th>Gaps</th><th>Priority</th><th></th></tr></thead><tbody>${rows||'<tr><td colspan="5">No data available.</td></tr>'}</tbody></table></div></section><aside class="gov-card"><h3 class="gov-card-title">AI focus areas</h3><div class="gap-insights">${gaps||'<div class="empty-state">No priority gaps.</div>'}</div></aside></div>`;this.container.querySelector('#dashboard-refresh')?.addEventListener('click',()=>this.load());this.container.querySelectorAll('[data-employee]').forEach(b=>b.addEventListener('click',()=>{document.getElementById('gap-employee-id').value=b.dataset.employee;this.onNavigate('gap-analysis')}));window.lucide?.createIcons?.()}#esc(v){return String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')}}
