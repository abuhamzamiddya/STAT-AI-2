class CompetencyGapComponent{constructor(apiClient,onTraining){this.apiClient=apiClient;this.onTraining=onTraining;this.form=document.getElementById('gap-analysis-form');this.input=document.getElementById('gap-employee-id');this.btn=document.getElementById('btn-analyze-gap');this.out=document.getElementById('gap-results-container');this.form?.addEventListener('submit',e=>{e.preventDefault();this.triggerAnalysis(this.input.value)});document.getElementById('gap-quick-select')?.addEventListener('click',e=>{const b=e.target.closest('[data-id]');if(b){this.input.value=b.dataset.id;this.triggerAnalysis(b.dataset.id)}})}async triggerAnalysis(id){if(!id)return;this.btn.disabled=true;try{const d=await this.apiClient.analyzeCompetencyGap(id);this.out.hidden=false;this.out.innerHTML=`<div class="gov-card"><h3>${this.#esc(d.employeeName)} • ${this.#esc(d.designation)}</h3><p>Competency match: <strong>${d.competencyMatchPercentage}%</strong></p><p>Missing skills: ${(d.missingSkills||[]).map(this.#esc).join(', ')||'None'}</p><button class="btn btn-saffron" id="gap-training">View targeted iGOT learning</button></div>`;document.getElementById('gap-training').onclick=()=>this.onTraining(d.employeeId)}catch(e){this.out.hidden=false;this.out.innerHTML=`<div class="gov-card empty-state">${this.#esc(e.message)}</div>`}finally{this.btn.disabled=false}}#esc(v){return String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')}}
